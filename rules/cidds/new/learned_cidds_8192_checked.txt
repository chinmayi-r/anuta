((Proto ≠ UDP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 67))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 53))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstIpAddr = dns))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 53))
((DstPt ≠ 22) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 67) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 53))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((DstIpAddr = dns) ∧ (DstPt = 137) ⇒ (Flags ≠ noflags))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((DstPt = 53) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 443))
((DstPt = 53) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcIpAddr ≠ public_p2p))
((DstPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 53))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 137))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 3))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 80))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto ≠ TCP) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((SrcPt ≠ 68) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 0))
((Flags = flags) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 80))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 80))
((Proto = TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((DstPt = 0) ∧ (Proto ≠ IGMP) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = private_broadcast))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 22))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 443))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ IGMP) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((DstPt = 443) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((Proto = UDP) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((SrcPt ≠ 22) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 22))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((DstPt ≠ 68) ⇒ (Flags = noflags) v (Flags = flags))
((Proto = ICMP) ⇒ (DstPt = 3))
((SrcPt ≠ 80) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 137))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((SrcPt ≠ 138) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 68))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt = 68) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((SrcPt = 137) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt = 138) ⇒ (DstPt ≠ 25))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto = TCP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ dns))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 3))
((DstIpAddr = public_p2p) ∧ (DstPt = 53) ⇒ (Flags ≠ noflags))
((Proto = TCP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 53))
((Proto = IGMP) ⇒ (SrcPt ≠ 67))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 80))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 443))
((SrcPt = 67) ⇒ (DstPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((Proto ≠ TCP) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((DstPt = 3) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 22))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcPt = 0) ⇒ (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 80))
((SrcPt ≠ 0) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 80))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 53))
((DstIpAddr = public_p2p) ∧ (DstPt = 138) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (DstPt ≠ 3) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((DstPt = 67) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 80))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstPt ≠ 67) ⇒ (Bytes ≥ 42xPackets))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 443))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 138))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 3))
((DstPt = 68) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 443))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((DstPt = 443) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 0))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcPt = 138) ⇒ (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ UDP) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstPt = 0) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt = 22) ⇒ (DstIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 0))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto ≠ ICMP) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 68))
((SrcPt = 138) ⇒ (Flags ≠ flags))
((DstIpAddr = dns) ∧ (Proto = UDP) ⇒ (DstPt = 53))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 3))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((SrcPt = 137) ⇒ (DstPt ≠ 80))
((DstIpAddr = dns) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 3))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 25))
((Proto = UDP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 67))
((DstIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (DstPt = 25) ⇒ (Flags = noflags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 0))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 138))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 67))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 3))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((DstPt = 3) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcIpAddr = public_p2p))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((Proto = UDP) ⇒ (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((DstIpAddr = dns) ∧ (DstPt = 0) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (DstPt = 3) ⇒ (Flags = flags))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 80))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((DstPt = 53) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 80))
((DstPt = 138) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 138) v (SrcPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((DstPt = 67) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_p2p) ∧ (DstPt = 53) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcPt ≠ 68))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 0))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = dns))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 22))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((DstIpAddr = public_p2p) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt = 22) ⇒ (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 80))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (SrcIpAddr = public_p2p) v (SrcPt ≠ 443))
((DstPt ≠ 3) ⇒ (Bytes ≥ 42xPackets))
((SrcPt = 137) ⇒ (DstPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((Proto = IGMP) ⇒ (DstPt = 0))
((Proto ≠ UDP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 138))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 137))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 0))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 0))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 443))
((DstIpAddr = public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcPt = 68) ⇒ (DstPt = 67))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 53))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 25))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto = TCP) ⇒ (DstPt ≠ 0))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((SrcPt ≠ 443) ⇒ (65535xPackets ≥ Bytes))
((Proto = TCP) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((DstPt = 138) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 22))
((SrcPt ≠ 80) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((Proto ≠ TCP) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 67))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 80))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((Proto ≠ IGMP) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 68))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 22))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((DstPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((DstPt ≠ 138) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 138))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = dns) v (SrcPt ≠ 53))
((DstPt = 137) ⇒ (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((SrcPt = 138) ⇒ (DstPt = 138))
((Proto = ICMP) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 53))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 53))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt = 53) ⇒ (Bytes ≥ 64xPackets))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcPt ≠ 53) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Flags ≠ flags) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 67))
((DstPt ≠ 53) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 22))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((DstPt = 3) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (SrcPt ≠ 0))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = dns))
((DstPt = 67) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 67))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 53))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 138))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (DstPt = 53) ⇒ (Flags = flags))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = public_p2p) v (SrcPt ≠ 443))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcPt = 443) ⇒ (DstPt ≠ 443))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstIpAddr ≠ public_p2p))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((DstPt = 22) ⇒ (Bytes ≥ 64xPackets))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 67))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcPt ≠ 0) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ UDP) ⇒ (Flags = flags) v (SrcIpAddr ≠ public_p2p))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((Proto = TCP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((Proto = IGMP) ⇒ (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 80))
((DstPt = 137) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt = 22) ⇒ (DstPt ≠ 3))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 3))
((DstPt = 3) ∧ (DstIpAddr ≠ dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = dns))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((DstPt ≠ 3) ⇒ (65535xPackets ≥ Bytes))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 3))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ dns))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcPt = 67) ⇒ (Flags ≠ flags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((Proto = UDP) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((DstPt = 138) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((Proto = IGMP) ⇒ (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((Proto = ICMP) ⇒ (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ⇒ (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 3))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((DstPt = 137) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 80))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 137))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (SrcPt = 0) v (DstPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 137))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 67))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 137))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 443))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 0))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (Flags = noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((SrcPt ≠ 67) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((DstPt = 443) ⇒ (Bytes ≥ 42xPackets))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 68))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((DstPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstIpAddr = dns))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstIpAddr = public_p2p))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((Proto = ICMP) ⇒ (DstPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto = UDP) ⇒ (SrcIpAddr = dns) v (SrcPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcPt = 53) ⇒ (Flags ≠ flags))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 0))
((DstIpAddr = private_p2p) ⇒ (65535xPackets ≥ Bytes))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 138))
((SrcPt ≠ 22) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcPt ≠ 0) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((Proto = TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (Flags = flags) v (DstPt ≠ 22))
((Proto = UDP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 67))
((DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto = UDP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcIpAddr ≠ dns))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (SrcPt = 0) v (DstPt ≠ 0))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 67))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((DstIpAddr = private_p2p) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 0))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((Flags ≠ noflags) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 0))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 22))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 138))
((DstPt = 67) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 68))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags = flags))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((DstPt = 0) ∧ (Proto = UDP) ⇒ (Flags = flags))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((Proto = UDP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ flags) v (SrcPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((SrcPt = 443) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 443))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 138))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 22))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 53))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (DstPt = 67) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcIpAddr ≠ public_p2p))
((DstPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((DstPt = 68) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((Proto ≠ TCP) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (DstPt = 22) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((SrcPt = 67) ∧ (DstPt ≠ 68) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((DstPt = 25) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (DstPt = 3) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 68))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((DstPt ≠ 25) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 3))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 443))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((DstPt = 53) ∧ (DstIpAddr ≠ dns) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((DstPt = 25) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((DstIpAddr = dns) ∧ (DstPt = 443) ⇒ (Flags = noflags))
((DstPt ≠ 25) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 68))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((SrcPt = 53) ⇒ (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((DstPt = 0) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstIpAddr = dns))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 22) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 25))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt = 53) ⇒ (DstIpAddr ≠ private_broadcast))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((SrcPt = 22) ⇒ (DstIpAddr ≠ public_p2p))
((SrcPt = 68) ∧ (DstPt ≠ 67) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcPt ≠ 53) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (Flags = flags))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 443))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((SrcIpAddr = dns) ⇒ (DstIpAddr = private_p2p))
((Proto ≠ ICMP) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 67))
((SrcPt ≠ 22) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 137) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((DstPt = 25) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcIpAddr ≠ dns))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt = 53))
((DstPt = 137) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 68))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 443))
((DstIpAddr = dns) ∧ (DstPt = 138) ⇒ (Flags = flags))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((SrcIpAddr = public_p2p) ⇒ (Flags ≠ noflags))
((SrcPt = 80) ⇒ (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 0))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 137))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 443))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 22))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((SrcIpAddr ≠ dns) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 22))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 443))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((DstIpAddr = private_p2p) ∧ (DstPt = 137) ⇒ (Flags = flags))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((DstPt = 53) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((DstPt = 443) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (SrcPt ≠ 67))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 22))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 53))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 25))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((DstPt = 53) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 67))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto = IGMP) ⇒ (DstIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 443))
((DstPt = 3) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 53))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((Proto = TCP) ⇒ (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 137))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 53))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 53))
((DstPt = 25) ⇒ (Bytes ≥ 64xPackets))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 68))
((SrcPt ≠ 443) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((SrcPt ≠ 0) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcPt = 138) ⇒ (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 53))
((SrcPt = 0) ∧ (Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (SrcIpAddr ≠ public_p2p))
((SrcPt = 80) ⇒ (DstIpAddr = private_p2p))
((Proto = UDP) ⇒ (DstPt ≠ 22))
((Proto = UDP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 80))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (DstPt = 67) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((DstPt = 3) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 22))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 443))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 443))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = public_p2p))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ TCP) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 137))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((DstPt ≠ 137) ⇒ (Flags = noflags) v (Flags = flags))
((Proto = ICMP) ⇒ (SrcPt = 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((Proto = ICMP) ⇒ (SrcPt ≠ 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcPt = 138) ⇒ (Flags = noflags))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = public_p2p))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 0))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 80))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = dns))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((SrcPt = 67) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((Proto ≠ UDP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((DstPt = 3) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((DstPt ≠ 25) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((DstPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (DstPt = 3) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 443) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr ≠ public_p2p) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 138))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((DstIpAddr = private_p2p) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((DstIpAddr = dns) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 53))
((DstPt = 443) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 443))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 138))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstIpAddr = dns))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 67))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 53))
((SrcPt ≠ 80) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 68))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 25))
((DstIpAddr = private_p2p) ∧ (DstPt = 0) ⇒ (Flags = flags))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 138))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (SrcPt = 0) v (DstPt ≠ 3))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 22))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = dns))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((DstPt = 67) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((Proto = TCP) ⇒ (SrcIpAddr = public_p2p) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 53))
((Proto = ICMP) ⇒ (DstPt ≠ 53))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 68))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt = 0) v (DstPt ≠ 0))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
(SrcPt ≠ 3) ∧ (SrcPt ≠ 8) ∧ (SrcPt ≠ 11) ∧ (SrcPt ≠ 25) ∧ (SrcPt ≠ 123) ∧ (SrcPt ≠ 8080)
((SrcPt = 68) ⇒ (DstPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 67))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((Proto = TCP) ⇒ (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 80))
((SrcPt = 0) ⇒ (DstPt = 0) v (DstIpAddr ≠ public_p2p))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 68))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ public_p2p))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((SrcPt = 80) ⇒ (DstPt ≠ 80))
((DstPt = 137) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((SrcPt ≠ 0) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = private_p2p))
((Proto = UDP) ⇒ (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((DstPt = 67) ⇒ (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((SrcPt = 0) ⇒ (DstIpAddr = dns) v (DstPt = 0))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr = private_broadcast))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 68))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 0))
((DstIpAddr = dns) ∧ (DstPt = 22) ⇒ (Flags ≠ flags))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcPt = 67) ⇒ (DstPt ≠ 80))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 22))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 137))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 80))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 0) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 22))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 443))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((DstPt = 137) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstPt ≠ 22))
((DstPt = 80) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcPt = 137) ⇒ (DstPt ≠ 3))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 80))
((DstIpAddr = private_p2p) ∧ (DstPt = 53) ⇒ (Flags = noflags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 138) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 137))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((SrcPt = 138) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 68))
((Proto ≠ UDP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((DstPt = 138) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcPt = 80) ⇒ (DstIpAddr ≠ private_broadcast))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((DstPt ≠ 80) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcPt ≠ 137) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((SrcPt ≠ 443) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 53))
((SrcPt = 80) ⇒ (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 22))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 68) ⇒ (Flags = flags) v (DstPt ≠ 25))
((DstPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (DstPt = 138) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 67))
((DstPt = 3) ∧ (SrcPt ≠ 0) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 443))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto = ICMP) ⇒ (SrcPt ≠ 443))
((DstPt ≠ 443) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr ≠ private_p2p))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 68))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 22))
((Proto ≠ UDP) ⇒ (Flags ≠ noflags) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto = TCP) ⇒ (SrcPt ≠ 68))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = dns))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 443))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 53))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (Flags ≠ noflags) v (SrcIpAddr ≠ public_p2p))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 22))
((SrcPt = 443) ⇒ (DstPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((DstPt = 0) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 0))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 80))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((SrcPt ≠ 53) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (DstPt = 138) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr = private_p2p))
((SrcPt ≠ 137) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 0))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcPt ≠ 53))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 0))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((SrcPt = 138) ⇒ (DstPt ≠ 0))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 0))
((SrcPt = 22) ⇒ (DstIpAddr ≠ private_broadcast))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 22))
((SrcPt = 53) ⇒ (DstPt ≠ 53))
((SrcPt ≠ 137) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 22))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((Proto = ICMP) ⇒ (DstPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((SrcPt = 80) ⇒ (DstIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 25))
((DstIpAddr ≠ private_broadcast) ⇒ (Bytes ≥ 42xPackets))
((SrcPt = 67) ⇒ (DstIpAddr ≠ public_p2p))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 0))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto = TCP) ⇒ (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 80))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 443))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 138))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 80))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 0))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = public_p2p) v (SrcPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcPt = 138) ⇒ (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcPt ≠ 137) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = private_broadcast))
((DstPt = 67) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 80))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((SrcPt = 443) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((DstPt = 0) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 53))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 3))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((Proto = TCP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr = private_p2p))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 138))
((SrcPt = 68) ⇒ (DstIpAddr ≠ public_p2p))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 68))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 80))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 137))
((DstPt = 0) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 68))
((SrcPt ≠ 137) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((DstIpAddr = dns) ⇒ (DstPt ≠ 25))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 53))
((DstPt = 53) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((Proto = UDP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 137))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((DstPt ≠ 68) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto = UDP) ⇒ (Flags = noflags))
((SrcPt = 138) ∧ (DstPt ≠ 138) ⇒ (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcPt ≠ 0))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 53))
((DstPt = 443) ⇒ (Flags ≠ noflags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 80))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 443))
((SrcPt = 22) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 80))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 0) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((SrcIpAddr = dns) ⇒ (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 22))
((DstPt = 3) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = private_broadcast))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 138))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 68))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((DstIpAddr = dns) ⇒ (Bytes ≥ 64xPackets))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 0))
((Proto = IGMP) ⇒ (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 22))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (DstPt = 68) ⇒ (Flags ≠ noflags))
((SrcPt = 80) ⇒ (DstPt ≠ 137))
((Proto = TCP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ public_p2p))
((DstPt = 68) ∧ (SrcPt ≠ 67) ⇒ (Flags = flags))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 137))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ⇒ (DstIpAddr = private_p2p))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 53))
((SrcPt = 80) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 137))
((SrcPt = 67) ⇒ (DstPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 137))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((Proto = IGMP) ⇒ (64xPackets ≥ Bytes))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 0))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 53))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt = 0) ⇒ (DstPt ≠ 138))
((Proto = TCP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 0))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((SrcPt ≠ 68) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 138))
((DstPt = 67) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 137))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 3))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 22))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((DstIpAddr = public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 443))
((DstIpAddr = dns) ⇒ (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr = private_p2p))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (Flags = flags) v (SrcPt ≠ 22))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto = UDP) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 67) ⇒ (65535xPackets ≥ Bytes))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((DstPt = 25) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 80))
((Proto = ICMP) ⇒ (DstIpAddr ≠ private_broadcast))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (DstPt = 53) ⇒ (Flags = noflags))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((SrcPt = 0) ⇒ (DstPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 0))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcPt = 137) ∧ (DstPt ≠ 137) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 138))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = private_broadcast) ∧ (DstPt = 67) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 68))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr = public_p2p))
((DstPt = 67) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ dns))
((Proto = ICMP) ⇒ (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 22) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcIpAddr ≠ dns))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 22))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 138) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((DstPt = 67) ∧ (SrcPt ≠ 68) ⇒ (Flags = flags))
((Proto ≠ UDP) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((DstPt ≠ 138) ⇒ (Flags = noflags) v (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 0))
((SrcPt ≠ 68) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 0))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcPt ≠ 67))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 138))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((DstPt = 53) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((DstPt = 0) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((DstPt ≠ 22) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcIpAddr ≠ public_p2p))
((SrcPt = 22) ⇒ (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 3))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto = ICMP) ⇒ (DstPt ≠ 443))
((DstPt = 443) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((SrcPt ≠ 138) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto = ICMP) ⇒ (SrcPt ≠ 22))
((DstPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 443))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((Proto = UDP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((DstIpAddr = private_p2p) ∧ (DstPt = 138) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (DstPt = 3) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((DstPt = 3) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcPt ≠ 67) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 25))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = dns))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((DstPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((Proto ≠ UDP) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 137))
((SrcPt ≠ 22) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_broadcast))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 3))
((SrcPt = 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((DstPt = 25) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 0))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((Proto ≠ TCP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 53))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((Proto = UDP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((DstPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 137))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = private_broadcast))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 67))
((DstPt ≠ 137) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 67))
((Proto = TCP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((DstPt = 443) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 443))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcPt ≠ 0) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 137) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 443))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = dns))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr = private_p2p) ∧ (DstPt = 443) ⇒ (Flags = noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((DstPt = 68) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((SrcPt = 0) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 67) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (DstPt = 0) ⇒ (Flags ≠ noflags))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 137))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((Flags ≠ noflags) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 80) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto = UDP) ⇒ (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstIpAddr = public_p2p))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (DstPt = 0) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = dns))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((DstPt = 68) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ flags) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 53))
((Proto = ICMP) ∧ (DstPt ≠ 3) ⇒ (Flags = flags))
(65535xPackets ≥ Bytes)
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto = TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 443))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = dns))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcIpAddr ≠ dns))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((DstPt = 53) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = public_p2p))
((Proto = TCP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((Proto = TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 0))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 80))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr = public_p2p) v (SrcPt ≠ 0))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((DstIpAddr = private_p2p) ∧ (DstPt = 3) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 443))
((DstIpAddr = private_p2p) ∧ (DstPt = 25) ⇒ (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 25))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 53))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 53))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 53))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ dns))
((DstPt ≠ 25) ⇒ (65535xPackets ≥ Bytes))
((DstPt = 68) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((Proto = TCP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((DstPt = 67) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (SrcPt = 0) v (DstPt ≠ 0))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 67))
((SrcIpAddr = dns) ⇒ (65535xPackets ≥ Bytes))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 80))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 0))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr = private_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstPt = 137) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 53))
((SrcPt ≠ 53) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 443))
((SrcPt ≠ 0) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 67))
((Proto ≠ UDP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 0))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt = 68) ⇒ (DstIpAddr = private_p2p))
((DstIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((DstPt = 53) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((DstPt = 80) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((DstPt ≠ 443) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((SrcPt = 53) ⇒ (DstPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((Proto ≠ UDP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 22))
((DstPt = 68) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 25))
((SrcPt = 137) ⇒ (DstIpAddr ≠ public_p2p))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 22))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((DstPt = 67) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 53))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 137))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 137))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 53))
((SrcPt = 53) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 68))
((Proto ≠ TCP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((DstIpAddr ≠ dns) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (SrcPt ≠ 443))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 22))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 138))
((DstPt ≠ 0) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 80))
((DstPt = 3) ∧ (Proto = UDP) ⇒ (Flags = flags))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((DstPt = 137) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((SrcPt = 80) ⇒ (DstPt ≠ 443))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((DstPt = 53) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((DstPt ≠ 53) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((SrcPt ≠ 68) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstPt ≠ 138) v (SrcIpAddr ≠ dns))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 67))
((Proto = ICMP) ⇒ (DstPt ≠ 138))
((DstPt = 138) ⇒ (Flags = noflags))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((DstPt = 53) ∧ (DstIpAddr ≠ dns) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcIpAddr ≠ dns))
((Proto = UDP) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 22))
((SrcPt = 0) ⇒ (DstPt ≠ 25))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 68))
((DstPt ≠ 22) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (Bytes ≥ 42xPackets))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 53))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstIpAddr = dns))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (SrcPt = 53) v (SrcIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (SrcIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 67))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcPt = 0) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstIpAddr = dns))
((SrcPt = 53) ⇒ (DstPt ≠ 0))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 443))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 443))
((SrcPt ≠ 137) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto = TCP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((DstPt = 68) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 67))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 0))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt = 68) ⇒ (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 443))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 0))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr = dns) v (SrcPt ≠ 0))
((SrcPt ≠ 53) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 137))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 25))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((Proto = TCP) ⇒ (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 80))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 443))
((Proto = TCP) ⇒ (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 80))
((Proto = UDP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 53))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 138))
((DstPt ≠ 68) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 137))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 138))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((Proto = IGMP) ⇒ (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 80))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 443) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (SrcPt = 0) v (DstPt ≠ 3))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ⇒ (DstIpAddr ≠ private_broadcast))
((SrcPt = 137) ⇒ (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((SrcPt = 80) ⇒ (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((DstPt ≠ 3) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 22))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcPt = 0) ⇒ (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 80))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 67))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 22))
((SrcIpAddr ≠ private_p2p) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 0) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 80))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((Proto = UDP) ⇒ (SrcPt ≠ 0))
(DstIpAddr ≠ private_any)
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((SrcPt = 443) ⇒ (DstIpAddr ≠ public_p2p))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((Proto = UDP) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((DstPt = 138) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (DstPt = 68) ⇒ (Flags = flags))
((Proto = UDP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 137))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 137) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 443))
((SrcPt ≠ 0) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = private_broadcast))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 67))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 68))
((SrcIpAddr = private_p2p) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((SrcPt ≠ 53) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = dns))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 137))
((SrcPt = 22) ⇒ (Flags ≠ noflags))
((Proto = UDP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((DstPt = 53) ∧ (DstIpAddr ≠ dns) ⇒ (Flags = noflags))
((DstPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((DstIpAddr = private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 22))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 68))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((Proto = UDP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 53))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((Proto = UDP) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 67))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 80))
((SrcPt = 137) ⇒ (65535xPackets ≥ Bytes))
((DstPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 80))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 138))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((Proto ≠ TCP) ⇒ (SrcPt = 0) v (DstPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 80))
((Proto = ICMP) ⇒ (SrcIpAddr = private_p2p))
((DstPt = 3) ⇒ (Flags = noflags))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 443))
((Proto = ICMP) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (Flags ≠ noflags) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 25))
((DstPt ≠ 138) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 67))
((DstPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr ≠ dns) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto = ICMP) ⇒ (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 53))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 80))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 22))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((SrcPt = 68) ⇒ (DstIpAddr ≠ dns))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 67))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 138))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 25))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 53))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((DstPt = 0) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((DstIpAddr = dns) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 0))
((DstIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 22))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 53))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (Flags = flags))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((DstIpAddr ≠ private_p2p) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 22))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 25))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = public_p2p))
((SrcPt = 0) ⇒ (Bytes ≥ 42xPackets))
((DstPt = 0) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 0))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 25))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 53))
((DstPt = 3) ∧ (DstIpAddr ≠ dns) ⇒ (Flags = flags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcPt = 138) ⇒ (DstPt ≠ 53))
((SrcPt = 68) ⇒ (DstPt ≠ 3))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((DstPt = 138) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((DstPt = 22) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (DstPt = 137) ⇒ (Flags ≠ noflags))
((Proto = UDP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (SrcPt ≠ 22))
((DstIpAddr = private_p2p) ∧ (DstPt = 443) ⇒ (Flags ≠ flags))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 137))
((Proto = UDP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 80))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 0))
((Proto = TCP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 53))
((Proto = IGMP) ⇒ (DstPt ≠ 3))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 138) v (SrcPt ≠ 443))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 22))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((DstPt = 138) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((SrcPt = 67) ⇒ (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcIpAddr = public_p2p))
((Proto = UDP) ⇒ (SrcPt ≠ 443))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((DstPt = 22) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 67))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((SrcPt = 0) ⇒ (DstPt = 0) v (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcPt ≠ 0) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((Proto ≠ IGMP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 22))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 68))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 67))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 0))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((DstPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt = 53) ⇒ (DstPt ≠ 80))
((SrcPt = 137) ⇒ (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = private_broadcast))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 68))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 80))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 138))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((SrcPt ≠ 80) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto = IGMP) ⇒ (DstIpAddr ≠ private_broadcast))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 443))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 67))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 80))
((Proto = ICMP) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((DstIpAddr = public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 80))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 53))
((SrcPt = 53) ⇒ (DstIpAddr = private_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 53))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstIpAddr = dns))
((SrcIpAddr = private_p2p) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 137))
((Proto ≠ UDP) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 67))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 25))
((SrcPt ≠ 138) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((Proto ≠ ICMP) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((SrcPt = 80) ⇒ (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 3))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcPt ≠ 22) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = public_p2p))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr ≠ private_p2p))
((DstPt ≠ 3) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 67))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 67))
((SrcPt ≠ 80) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((DstIpAddr = dns) ⇒ (DstPt ≠ 138))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 443))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 53))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((SrcIpAddr = public_p2p) ⇒ (DstIpAddr ≠ public_p2p))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((DstPt = 0) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ⇒ (65535xPackets ≥ Bytes))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 0))
((SrcPt ≠ 138) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 137))
((SrcPt = 67) ⇒ (DstPt ≠ 443))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 137))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((DstPt = 67) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 443))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 68))
((DstIpAddr = private_p2p) ∧ (DstPt = 25) ⇒ (Flags = noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 25))
((DstIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((DstPt = 137) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((DstPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ dns))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 68))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_p2p))
((Proto = IGMP) ⇒ (SrcIpAddr = private_p2p))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 68))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((SrcPt = 137) ⇒ (DstPt ≠ 53))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((DstPt = 80) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((Proto = UDP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 0))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto = IGMP) ⇒ (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto = TCP) ⇒ (Bytes ≥ 42xPackets))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 137))
((Proto = UDP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((DstPt = 0) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcPt = 53) ⇒ (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((SrcPt ≠ 22) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 67))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 22))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcIpAddr = dns))
((SrcPt ≠ 138) ⇒ (Flags = flags) v (DstPt ≠ 25))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstIpAddr = dns))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (Flags ≠ noflags))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr = private_p2p))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((Proto = TCP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((DstPt = 53) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 22))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((SrcPt = 67) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = dns))
((DstIpAddr ≠ private_broadcast) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 68))
((Flags = flags) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 22) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcPt = 67) ⇒ (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 68))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 68))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 68))
((Proto = IGMP) ⇒ (Flags = noflags))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstIpAddr ≠ private_p2p))
((DstPt = 25) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 80))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 0))
((SrcPt = 138) ⇒ (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 22))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((SrcPt = 67) ∧ (DstPt ≠ 68) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 138))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcIpAddr ≠ public_p2p))
((SrcPt = 138) ⇒ (DstPt ≠ 3))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 68))
((DstPt = 138) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((Proto = ICMP) ⇒ (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 0))
((Proto ≠ TCP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((Proto = TCP) ⇒ (SrcPt ≠ 0))
((Proto ≠ UDP) ⇒ (SrcIpAddr ≠ dns))
((DstPt = 3) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (SrcPt = 67))
((DstPt = 53) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (DstPt = 53) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((DstPt = 68) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 68))
((Proto = TCP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((Proto = UDP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 0))
((SrcPt = 0) ⇒ (DstPt ≠ 443))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 53))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 53))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 3))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((SrcPt = 67) ⇒ (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((Proto = IGMP) ⇒ (DstIpAddr ≠ private_p2p))
((DstPt = 53) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 0))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 68))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((DstPt = 67) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 67))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 68))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 68))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ public_p2p))
((DstPt = 3) ∧ (Proto = UDP) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 0))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 138))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((DstPt = 80) ⇒ (Flags ≠ noflags))
((DstPt = 0) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 22))
((DstPt = 0) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((DstIpAddr ≠ public_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((Proto = IGMP) ⇒ (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 3))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 138))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 0))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 67))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 137) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((DstIpAddr = dns) ∧ (DstPt = 22) ⇒ (Flags = noflags))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 443))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 138))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 138))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((Proto ≠ UDP) ⇒ (DstPt ≠ 67))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 22))
((DstPt = 443) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcPt ≠ 68) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (65535xPackets ≥ Bytes))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((DstPt = 137) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (SrcPt = 0) v (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 68))
((SrcPt = 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((DstPt = 22) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 53))
((DstPt = 25) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((DstPt ≠ 67) ⇒ (65535xPackets ≥ Bytes))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (DstPt = 22) ⇒ (Flags ≠ flags))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((SrcPt ≠ 138) ⇒ (Bytes ≥ 42xPackets))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 53))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcPt = 0) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 22))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = dns))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 68))
((DstPt = 0) ⇒ (Flags = noflags))
((SrcPt = 22) ⇒ (DstPt ≠ 443))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 443))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 53))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ dns))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 138))
((Proto = UDP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((Proto = UDP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 0))
((SrcPt ≠ 68) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 0))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt = 67) ⇒ (DstPt ≠ 25))
((Proto ≠ TCP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 0))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 0) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 443))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 0) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags) v (DstPt ≠ 443))
((DstPt = 22) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 67))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((SrcPt ≠ 80) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt = 138) ∧ (DstPt ≠ 138) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (SrcPt ≠ 68))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 68))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 68))
((DstPt = 22) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 137))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstIpAddr = dns))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 68) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 22))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 22))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((DstPt = 53) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 80))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 0))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto ≠ UDP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 22))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 138))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 53))
((SrcPt ≠ 67) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 0))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((DstPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 53))
((SrcPt = 138) ⇒ (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstIpAddr = dns))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto = TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((Proto = TCP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((SrcPt = 137) ⇒ (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (DstPt ≠ 443))
((DstPt ≠ 53) ⇒ (Bytes ≥ 42xPackets))
((DstPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 137))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((DstPt = 0) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((DstPt = 53) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((DstPt = 3) ∧ (SrcPt ≠ 0) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((Proto ≠ TCP) ⇒ (SrcIpAddr = dns) v (SrcPt ≠ 53))
((Proto = IGMP) ⇒ (SrcPt ≠ 443))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 138))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 443))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstIpAddr = dns))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 67))
((Proto = TCP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 0))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((SrcPt ≠ 22) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((SrcPt = 0) ∧ (Proto ≠ ICMP) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 137))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((SrcPt ≠ 53) ⇒ (Flags = flags) v (DstPt ≠ 25))
((DstIpAddr = dns) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((Proto ≠ UDP) ⇒ (Flags = flags) v (SrcIpAddr = private_p2p))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 53))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcPt = 0) ⇒ (DstPt ≠ 80))
((DstPt = 138) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 80))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 22))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 137))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((DstPt = 53) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((SrcPt = 80) ⇒ (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt = 80) ⇒ (DstIpAddr ≠ dns))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((DstPt = 25) ⇒ (Flags = flags))
((DstPt = 138) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((DstIpAddr = private_p2p) ∧ (DstPt = 53) ⇒ (Flags ≠ flags))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 67))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 22))
((SrcPt ≠ 138) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((Proto ≠ UDP) ∧ (SrcPt ≠ 0) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 443))
((DstPt = 138) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((DstPt = 53) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 25))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((DstPt = 138) ∧ (SrcPt ≠ 138) ⇒ (Flags = flags))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 0))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((DstPt = 68) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 53))
((Proto = UDP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((DstPt = 138) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (DstPt = 68) ⇒ (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = private_broadcast))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 80))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 53))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((DstPt = 0) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 25))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p))
((DstPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((Proto = TCP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ public_p2p))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr = public_p2p))
((DstPt ≠ 53) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto ≠ UDP) ⇒ (SrcPt = 0) v (DstPt ≠ 0))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = dns))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 138) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 22) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((DstIpAddr = dns) ∧ (DstPt = 67) ⇒ (Flags ≠ noflags))
((DstPt = 137) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((DstPt = 3) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 25))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcPt = 53) ⇒ (DstPt ≠ 25))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 137))
((SrcIpAddr ≠ private_p2p) ∧ (SrcPt ≠ 53) ⇒ (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 53))
((Proto ≠ UDP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 137))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (DstPt = 67) ⇒ (Flags = flags))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 80))
((SrcPt ≠ 22) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 443))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcPt = 53) ⇒ (DstPt ≠ 138))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcIpAddr = dns) ⇒ (DstIpAddr ≠ dns))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstPt = 68))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ flags) v (SrcPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((DstPt = 137) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((SrcPt ≠ 67) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 22))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((SrcPt = 443) ⇒ (DstPt ≠ 25))
((SrcPt = 68) ⇒ (DstPt ≠ 80))
((DstPt = 67) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ dns))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 22))
((Proto ≠ TCP) ⇒ (SrcPt = 53) v (SrcIpAddr ≠ dns))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((SrcPt ≠ 80) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = private_p2p))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 67))
((Proto ≠ TCP) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 3))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (SrcIpAddr ≠ dns))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 22))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((SrcPt ≠ 138) ⇒ (Flags = flags) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((DstPt = 53) ∧ (DstIpAddr ≠ dns) ⇒ (Flags ≠ noflags))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 80))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((DstPt = 67) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((Proto ≠ UDP) ⇒ (SrcPt = 0) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 0))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
(SrcIpAddr ≠ private_broadcast) ∧ (SrcIpAddr ≠ private_any)
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((Proto = ICMP) ⇒ (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 53))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr = public_p2p) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 22))
((DstPt = 443) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((DstPt = 22) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcPt = 22) ⇒ (Bytes ≥ 64xPackets))
((SrcPt = 138) ⇒ (DstIpAddr = private_broadcast))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 137))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 67))
((SrcPt = 22) ⇒ (DstPt ≠ 25))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((SrcPt = 137) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((DstPt = 53) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 443))
((DstIpAddr = dns) ⇒ (DstPt ≠ 80))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((DstPt = 137) ⇒ (Flags = noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 25))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 443))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 22))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcIpAddr ≠ dns))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 0))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 0))
((DstPt = 138) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 80))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt = 53))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 53))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcPt = 443) ⇒ (DstPt ≠ 3))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 443))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcPt ≠ 80) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcIpAddr ≠ dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((Proto = UDP) ⇒ (DstPt ≠ 138) v (SrcIpAddr ≠ dns))
((DstPt = 137) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 443))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 3))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 138))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 0))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 3))
((DstPt = 137) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((Proto ≠ UDP) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 138))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = private_broadcast))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 138))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((Proto = ICMP) ⇒ (DstIpAddr ≠ public_p2p))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_broadcast))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 68))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 0))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 22))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = private_broadcast))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (Flags = flags))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcIpAddr ≠ public_p2p))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = dns))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((DstPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((DstPt = 67) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 138))
((SrcPt = 68) ⇒ (DstPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((SrcPt = 22) ⇒ (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (DstPt = 53) ⇒ (Flags ≠ flags))
((DstPt = 138) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 0))
((SrcPt ≠ 22) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 3))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((DstPt ≠ 80) ⇒ (65535xPackets ≥ Bytes))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 22))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 22))
((SrcPt ≠ 68) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = private_p2p))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto = IGMP) ⇒ (DstPt ≠ 68))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((DstPt = 138) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((Proto = TCP) ⇒ (SrcPt ≠ 67))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 80))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 68))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 138))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((DstIpAddr = dns) ⇒ (DstPt ≠ 0))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 25))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags ≠ noflags))
((DstPt = 137) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 68))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 67))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((DstPt ≠ 443) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = private_broadcast))
((DstPt = 53) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 0))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcPt = 443) ⇒ (DstPt ≠ 22))
((SrcIpAddr = dns) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 138))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 22))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 22) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstPt = 3) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((DstPt = 138) ∧ (SrcPt ≠ 138) ⇒ (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 443))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 80))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 53))
((SrcPt ≠ 67) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 25))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = dns))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt = 443) ⇒ (65535xPackets ≥ Bytes))
((Proto ≠ UDP) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 138))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 443))
((DstPt = 68) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((DstPt = 80) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 138))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags) v (SrcPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 67))
((SrcPt = 138) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcIpAddr ≠ public_p2p))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 80))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 443))
((Proto = ICMP) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 80) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((DstIpAddr = dns) ∧ (DstPt = 443) ⇒ (Flags ≠ flags))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 22))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstPt = 25) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((DstPt = 68) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 67))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcPt ≠ 137))
((SrcIpAddr = dns) ⇒ (Flags = noflags))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 137))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto = UDP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = dns))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 68))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 22))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcPt = 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 67))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 25))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = public_p2p))
((DstPt = 53) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 22))
((DstIpAddr = dns) ⇒ (DstPt ≠ 22))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 137))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 0))
((DstIpAddr = private_broadcast) ∧ (DstPt = 0) ⇒ (Flags ≠ noflags))
((DstPt = 138) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto = TCP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 443))
((SrcPt = 137) ⇒ (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 53))
((DstPt = 443) ⇒ (65535xPackets ≥ Bytes))
((DstPt = 22) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstPt = 68) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 0))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcIpAddr ≠ private_p2p) ∧ (SrcPt ≠ 53) ⇒ (Flags = flags))
((DstPt = 53) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 3))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 22))
((DstPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcIpAddr ≠ dns))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 0))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcIpAddr ≠ dns))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((DstPt = 138) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 0) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcIpAddr = dns) ⇒ (SrcPt = 53))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 137))
((SrcPt ≠ 22) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 137))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstPt ≠ 0) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((DstPt = 80) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((Proto ≠ UDP) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((SrcPt = 68) ⇒ (65535xPackets ≥ Bytes))
((SrcPt = 137) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstIpAddr = dns))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 137))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (SrcIpAddr ≠ dns))
((Proto = TCP) ⇒ (SrcIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 138))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcIpAddr ≠ dns))
((SrcPt = 22) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 25))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 68))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 138))
((Proto = IGMP) ⇒ (DstPt ≠ 80))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 137))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 80))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 80))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 67))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 53))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = private_p2p))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 0))
((SrcPt ≠ 443) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt = 443) ⇒ (DstIpAddr ≠ private_broadcast))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((DstPt = 67) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (SrcPt = 0) v (DstPt ≠ 3))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((DstPt = 68) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = dns) v (SrcPt ≠ 53))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 3))
((SrcPt = 53) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((SrcPt = 68) ⇒ (DstPt ≠ 53))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 22))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (SrcPt ≠ 137))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((DstPt = 443) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
(Bytes ≥ 42xPackets)
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 67))
((DstPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto = TCP) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 443) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 443))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 53))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 138))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 3))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((DstIpAddr = private_broadcast) ⇒ (Flags = noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstIpAddr = dns))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 53))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 68))
((Proto ≠ ICMP) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr = private_p2p))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((Proto = TCP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((DstPt = 0) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 138))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((SrcPt = 443) ⇒ (DstPt ≠ 53))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 0))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 0))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 3))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 443))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 53))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 0))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 80))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 138))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 67))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcPt = 0) ⇒ (DstPt ≠ 53))
((Proto = IGMP) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 68))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((SrcPt = 137) ⇒ (DstPt ≠ 68))
((Proto = TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 137))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 68))
((SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr ≠ dns))
((SrcPt = 67) ⇒ (DstIpAddr = private_p2p))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((Proto = UDP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p))
((SrcPt = 80) ⇒ (DstPt ≠ 0))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto = ICMP) ⇒ (SrcIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((Proto = ICMP) ⇒ (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 443))
((SrcPt ≠ 53) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((DstPt = 3) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstPt = 67) ∧ (SrcPt ≠ 68) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((DstPt = 68) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((SrcPt = 68) ⇒ (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 68))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
(DstPt ≠ 8) ∧ (DstPt ≠ 11) ∧ (DstPt ≠ 123) ∧ (DstPt ≠ 8080)
((SrcPt ≠ 68) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((DstIpAddr = dns) ∧ (DstPt = 25) ⇒ (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 67))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 137))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 80))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 0))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
((SrcPt = 0) ⇒ (DstIpAddr ≠ private_broadcast))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 138))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 80) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 0))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = private_broadcast))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 3))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((Proto = TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 53))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((DstPt = 53) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 0))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 68))
((DstPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((Proto = ICMP) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 137))
((DstPt = 53) ⇒ (Bytes ≥ 64xPackets))
((SrcPt ≠ 22) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 22))
((DstPt = 25) ∧ (SrcPt = 80) ⇒ (Flags = noflags))
((DstPt = 68) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 67))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((SrcPt = 138) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstPt = 138) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 67))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((DstIpAddr = private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 25))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt = 443) ⇒ (DstIpAddr = private_p2p))
((DstPt = 138) ⇒ (Bytes ≥ 64xPackets))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr = dns))
((Proto ≠ UDP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((DstIpAddr = dns) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((Proto ≠ UDP) ⇒ (Flags = flags) v (DstIpAddr ≠ private_p2p))
((DstPt = 137) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 137))
((SrcPt = 67) ⇒ (DstIpAddr ≠ dns))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((SrcPt = 80) ⇒ (Flags = flags))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 138))
((DstPt = 0) ∧ (SrcPt ≠ 0) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 22))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 67))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 67))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 53))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr ≠ private_broadcast))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((SrcPt ≠ 67) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((DstPt = 68) ∧ (SrcPt ≠ 67) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((Proto = UDP) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 443))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 25))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((DstPt ≠ 137) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 22))
((SrcIpAddr = public_p2p) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 0))
((Proto = UDP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((SrcPt = 443) ∧ (Proto ≠ TCP) ⇒ (DstPt = 25))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto = ICMP) ⇒ (DstPt ≠ 68))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 138))
((SrcPt = 53) ⇒ (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((SrcPt ≠ 68) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((SrcPt ≠ 67) ⇒ (Flags = flags) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (SrcIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 68))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 25))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 67))
((SrcPt = 137) ⇒ (Flags = noflags))
((DstIpAddr = dns) ⇒ (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 68))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((DstPt = 25) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 22))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 0))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstPt = 80))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 137))
((DstIpAddr = dns) ⇒ (DstPt = 53) v (Flags = noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 25))
((DstPt ≠ 138) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 137))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 68))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstIpAddr = private_p2p))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((Proto = IGMP) ⇒ (SrcPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = dns))
((SrcPt = 53) ⇒ (Flags = noflags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt = 0) ⇒ (DstPt ≠ 137))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((DstPt = 3) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 138))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = private_p2p))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 0))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 138))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto = TCP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((Proto = TCP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 443))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcPt = 53) ⇒ (DstPt ≠ 3))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 22))
((DstPt = 137) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstIpAddr = dns))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 68) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((Proto = TCP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 443))
((DstPt = 68) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 22))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 25))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 68))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 0))
((SrcIpAddr = dns) ⇒ (Flags ≠ flags))
((Proto = IGMP) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = dns))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 443))
((SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr ≠ public_p2p))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 53))
((DstPt = 3) ⇒ (Flags ≠ flags))
((DstIpAddr ≠ private_p2p) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((Proto ≠ IGMP) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 68))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 53))
((DstPt ≠ 67) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 443) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 22))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 68))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 0))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 53))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 80))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 0))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 67))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcIpAddr ≠ dns))
((DstIpAddr = dns) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((DstIpAddr = dns) ⇒ (DstPt = 53) v (Flags ≠ flags))
((DstPt = 3) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 25))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 53) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = dns))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 25))
((SrcPt ≠ 53) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (Flags = flags))
((DstIpAddr = private_p2p) ∧ (DstPt = 53) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 53))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((DstPt = 0) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstIpAddr = public_p2p))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 67))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (DstPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstIpAddr = public_p2p))
((DstPt = 53) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 138))
((DstPt = 0) ⇒ (Flags ≠ flags))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 67))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 0))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((Proto = ICMP) ⇒ (SrcPt ≠ 68))
((SrcPt ≠ 443) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((Proto = TCP) ⇒ (DstPt ≠ 443) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((DstPt = 67) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (SrcPt = 68) v (DstPt ≠ 67))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((SrcPt ≠ 138) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 138))
((Proto = IGMP) ⇒ (DstPt ≠ 53))
((DstIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 67))
((DstIpAddr ≠ public_p2p) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 138))
((Proto = TCP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstIpAddr = public_p2p))
((SrcPt = 443) ⇒ (DstPt ≠ 68))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((SrcPt = 80) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 67))
((DstPt = 3) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 53))
((Proto = UDP) ⇒ (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 443))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 53))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 137))
((DstPt = 68) ⇒ (Flags ≠ flags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 137))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 138))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 53))
((SrcPt = 68) ⇒ (DstPt ≠ 25))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((SrcPt ≠ 68) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((DstPt = 80) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
((DstPt = 53) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcPt ≠ 68))
((DstPt = 3) ∧ (Proto ≠ ICMP) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 0))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 22))
((Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (DstPt = 67) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto = IGMP) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ ICMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 68))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 22))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 80))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 68))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((DstPt = 138) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((DstPt = 80) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (DstPt = 68) ⇒ (Flags = flags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt = 68) ∧ (DstPt ≠ 67) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((SrcPt = 0) ⇒ (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 443))
((DstPt = 68) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((DstPt = 3) ∧ (Proto ≠ ICMP) ⇒ (Flags ≠ noflags))
((DstPt = 22) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 25))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((DstIpAddr = private_broadcast) ∧ (DstPt = 68) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (DstPt = 80) ⇒ (Flags ≠ flags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstIpAddr = private_broadcast))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 443))
((DstIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 3))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 22))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 67))
((SrcPt = 137) ⇒ (DstPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 22))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 138))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 22))
((Proto = TCP) ⇒ (DstPt ≠ 80) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 138))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ private_broadcast))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 443))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 22))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 3))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (DstPt = 137) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((DstPt ≠ 443) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt = 137) ⇒ (DstPt = 137))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 53))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 443))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = dns))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 67))
((DstPt = 137) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (SrcIpAddr ≠ dns))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 0))
((Proto = IGMP) ⇒ (SrcPt ≠ 53))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 53))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((SrcPt = 443) ⇒ (DstPt ≠ 80))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 443))
((DstPt = 80) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((DstPt = 25) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 0))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((DstPt = 53) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((Proto = TCP) ⇒ (DstIpAddr ≠ private_broadcast))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 137))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 53))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto = UDP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 138))
((SrcPt = 443) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt = 68) ⇒ (DstPt ≠ 68))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 67))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((Proto = TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((DstPt = 53) ∧ (SrcPt = 53) ⇒ (Flags = flags))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((Proto = IGMP) ⇒ (SrcIpAddr ≠ public_p2p))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 3))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr ≠ private_p2p))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 138))
((DstPt ≠ 80) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((SrcPt = 137) ⇒ (DstIpAddr ≠ dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((SrcIpAddr ≠ dns) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0))
((SrcPt = 443) ⇒ (Flags = flags))
((SrcPt = 67) ⇒ (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 68))
((Proto = IGMP) ∧ (DstPt ≠ 0) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 53))
((DstPt = 443) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (DstPt ≠ 68))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (SrcPt = 0) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((Proto = TCP) ⇒ (DstPt ≠ 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 68))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 3))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (Flags = flags) v (SrcPt ≠ 443))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((DstPt = 22) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 0))
((SrcPt = 68) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((Proto = UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcIpAddr ≠ dns))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 67))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 22))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 138))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((DstPt = 22) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr = private_p2p))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((DstPt ≠ 0) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (Flags ≠ noflags))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 53))
((DstPt ≠ 22) ⇒ (65535xPackets ≥ Bytes))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 0))
((DstIpAddr = dns) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (Proto = TCP) ⇒ (DstPt = 53))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 137))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstPt ≠ 137) v (SrcIpAddr ≠ dns))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 67))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcPt ≠ 137))
((SrcPt ≠ 67) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((DstIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (SrcIpAddr ≠ public_p2p))
((Proto = UDP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (Flags = flags) v (SrcPt ≠ 22))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 137))
((SrcPt = 68) ⇒ (Flags = noflags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((DstPt = 25) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((DstIpAddr = dns) ⇒ (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 67))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 22))
((Proto = UDP) ⇒ (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcPt ≠ 0))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((SrcPt = 443) ⇒ (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 443))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 443))
((SrcPt ≠ 0) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcPt = 0) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 3))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 22))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (SrcPt ≠ 138))
((Flags = noflags) ⇒ (65535xPackets ≥ Bytes))
((Proto = UDP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 137))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 0))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((DstPt = 53) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto = UDP) ⇒ (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcPt = 0) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ public_p2p))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 0))
((Proto = IGMP) ∧ (SrcPt = 137) ⇒ (DstPt = 443))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((Proto = TCP) ∧ (SrcPt = 138) ⇒ (DstPt = 25))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 25))
((Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 53))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt = 67) ⇒ (DstPt = 68))
((Proto ≠ TCP) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 137))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 80))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 25))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 67))
((Proto = ICMP) ∧ (DstIpAddr ≠ dns) ⇒ (DstPt = 80))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 3))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 443))
((SrcPt ≠ 137) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = noflags))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 80))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((DstPt = 80) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 0))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcPt = 0) ∧ (Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = dns))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstPt = 3))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ flags) v (SrcPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 3))
((Proto = IGMP) ⇒ (SrcPt ≠ 80))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstIpAddr = dns))
((Proto ≠ IGMP) ⇒ (SrcPt = 138) v (DstPt ≠ 138))
((SrcPt = 68) ⇒ (Bytes ≥ 64xPackets))
((DstIpAddr = private_p2p) ∧ (Proto = IGMP) ⇒ (DstPt = 68))
((SrcPt = 0) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (SrcIpAddr ≠ dns))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (Flags = noflags) v (DstPt ≠ 3))
((Proto = UDP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (SrcPt ≠ 53))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((Proto = ICMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcPt ≠ 22) ⇒ (Bytes ≥ 42xPackets))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 137))
((Proto = UDP) ⇒ (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((DstPt = 443) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 53))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 138))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstPt = 67))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((DstPt = 53) ∧ (Proto = IGMP) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (Proto = IGMP) ⇒ (DstPt = 68))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 3))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 67))
((DstPt = 137) ∧ (SrcPt ≠ 137) ⇒ (Flags = flags))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((DstPt = 53) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 137) v (SrcPt ≠ 138))
((DstIpAddr = dns) ∧ (DstPt = 137) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((Proto = TCP) ⇒ (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 443))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((Proto ≠ UDP) ⇒ (DstPt ≠ 68))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((DstPt = 53) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((Proto = UDP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (DstPt = 138) v (SrcPt ≠ 138))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 67))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 68))
((Proto ≠ ICMP) ⇒ (DstPt = 68) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcPt ≠ 443) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 67))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ dns))
((Proto ≠ ICMP) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((DstIpAddr = private_p2p) ∧ (DstPt = 3) ⇒ (Flags = flags))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 67))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstIpAddr = public_p2p))
((DstPt = 53) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt = 80) ⇒ (DstPt ≠ 53))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 138))
((DstPt = 138) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = noflags) v (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((DstPt = 80) ∧ (SrcPt = 443) ⇒ (Flags ≠ flags))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = dns))
((SrcPt = 138) ⇒ (DstPt ≠ 67))
((Proto = ICMP) ⇒ (DstPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 80))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 0))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((SrcIpAddr = dns) ∧ (SrcPt = 138) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (Flags ≠ noflags))
((DstPt = 0) ∧ (DstIpAddr ≠ public_p2p) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 0))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 443))
((SrcIpAddr ≠ private_p2p) ⇒ (Bytes ≥ 42xPackets))
((DstPt = 0) ∧ (Proto ≠ IGMP) ⇒ (Flags ≠ noflags))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 68) v (SrcPt ≠ 80))
((DstPt = 68) ∧ (Proto ≠ UDP) ⇒ (Flags = flags))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 138))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcPt = 137) ∧ (DstPt ≠ 137) ⇒ (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (SrcIpAddr ≠ dns))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 25))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 137))
((DstIpAddr = dns) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 53) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (DstPt = 53) ⇒ (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 22) ⇒ (DstPt = 68))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (SrcPt = 137) v (DstPt ≠ 137))
((Proto = IGMP) ⇒ (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (DstPt = 0))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 0))
((SrcIpAddr ≠ dns) ⇒ (Flags = flags) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 137))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 0))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((Proto = TCP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((SrcPt = 138) ⇒ (DstIpAddr ≠ dns))
((Proto = IGMP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = private_p2p))
((SrcPt = 80) ∧ (Proto ≠ TCP) ⇒ (DstPt = 68))
((Proto = TCP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 3))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstIpAddr = private_broadcast))
((DstIpAddr = dns) ⇒ (DstPt ≠ 443))
((Proto = IGMP) ⇒ (SrcPt ≠ 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 3))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 137))
((DstIpAddr ≠ dns) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr = public_p2p))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 25) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 3))
((SrcPt ≠ 138) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 22))
((DstPt = 0) ∧ (SrcPt ≠ 0) ⇒ (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 138))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (Flags = noflags))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 68))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 137))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 443))
((SrcIpAddr = private_p2p) ⇒ (Flags = flags) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 80))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 53))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 137))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 137))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 80))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ dns))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstIpAddr = dns))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 137))
((SrcPt = 53) ⇒ (DstPt ≠ 22))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 67))
((Proto = TCP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ public_p2p))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr = private_p2p))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 68))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((DstPt = 68) ⇒ (Flags = noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((DstIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((DstPt = 3) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((SrcPt = 22) ⇒ (DstPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt = 22) ⇒ (DstIpAddr = private_p2p))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 68))
((DstPt = 68) ⇒ (65535xPackets ≥ Bytes))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 53))
((DstIpAddr = dns) ∧ (Proto ≠ ICMP) ⇒ (DstPt = 53))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstIpAddr = private_p2p))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 22))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((SrcPt = 138) ⇒ (DstPt ≠ 80))
((SrcPt = 138) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (DstPt = 80))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (Flags ≠ noflags))
((SrcPt ≠ 53) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 443))
((Proto = UDP) ∧ (SrcPt = 443) ⇒ (DstPt = 443))
((SrcPt = 22) ⇒ (DstPt ≠ 138))
((DstIpAddr = private_p2p) ∧ (DstPt = 0) ⇒ (Flags ≠ noflags))
((DstIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 68))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 138))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 22))
((SrcIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstIpAddr = private_broadcast))
((Proto = IGMP) ∧ (DstPt ≠ 0) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 0))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstIpAddr ≠ private_p2p))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((Proto = TCP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (Flags = flags) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 138))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 0))
((DstPt = 22) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 137))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 443))
((DstPt = 67) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags) v (SrcPt ≠ 137))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 0) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((SrcPt = 0) ⇒ (DstIpAddr = public_p2p) v (DstIpAddr = dns))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((DstIpAddr = public_p2p) ∧ (SrcPt = 68) ⇒ (DstPt = 3))
((DstPt = 3) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((DstPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((Proto ≠ UDP) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcPt ≠ 137) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 53))
((DstPt = 80) ∧ (SrcPt = 22) ⇒ (Flags ≠ flags))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((SrcPt = 443) ⇒ (DstIpAddr ≠ dns))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 138))
((SrcPt ≠ 80) ⇒ (Bytes ≥ 42xPackets))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (Flags ≠ flags) v (SrcPt ≠ 53))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 0))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 67) ⇒ (DstPt = 443))
((Proto = UDP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 68))
((Proto = ICMP) ⇒ (SrcPt ≠ 53))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 138))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstIpAddr = dns))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 138))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 22))
((SrcPt = 22) ⇒ (DstPt ≠ 0))
((Proto = TCP) ∧ (SrcPt = 0) ⇒ (DstPt = 53))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 22))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcIpAddr ≠ dns))
((Proto = TCP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 443))
((SrcPt = 67) ⇒ (DstPt ≠ 53))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (DstPt ≠ 137))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto = TCP) ⇒ (SrcPt ≠ 138))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 137))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (Flags = noflags))
((SrcPt = 80) ⇒ (Flags ≠ noflags))
((DstPt = 137) ∧ (SrcPt ≠ 137) ⇒ (Flags ≠ noflags))
((DstPt ≠ 80) ⇒ (Flags = noflags) v (Flags = flags))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 137))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ public_p2p))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 0) v (SrcIpAddr ≠ dns))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 25))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((Proto = ICMP) ⇒ (DstPt ≠ 22))
((Proto = IGMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 67))
((SrcPt ≠ 67) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstIpAddr = dns))
((SrcPt = 137) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 68))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (65535xPackets ≥ Bytes))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 137))
((DstPt = 137) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 443))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstIpAddr = public_p2p))
((Proto = TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 22))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (Flags = flags))
((DstIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 3))
((DstPt = 0) ⇒ (64xPackets ≥ Bytes))
((DstPt = 67) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((Proto = TCP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 443))
((DstPt = 25) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((DstPt = 137) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 443))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (Flags = noflags) v (SrcPt ≠ 138))
((Proto = ICMP) ∧ (SrcPt = 68) ⇒ (DstPt = 0))
((SrcIpAddr = dns) ∧ (SrcPt = 80) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 68))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr = private_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcPt = 443) ⇒ (DstPt ≠ 67))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcIpAddr = dns))
((Proto = TCP) ∧ (SrcPt = 137) ⇒ (DstPt = 0))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((SrcPt ≠ 137) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 67))
((DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags) v (DstPt ≠ 22))
((Proto = TCP) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((DstPt = 3) ⇒ (Bytes ≥ 64xPackets))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto = UDP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 68))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 80))
((Proto = UDP) ⇒ (DstPt ≠ 137) v (SrcIpAddr ≠ dns))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 0))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((SrcPt = 22) ⇒ (DstPt ≠ 67))
((Proto = ICMP) ∧ (SrcPt = 137) ⇒ (DstPt = 22))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 80) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 80))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 138))
((Proto = ICMP) ∧ (SrcPt = 22) ⇒ (DstPt = 22))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (SrcPt = 80))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 443))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (Flags ≠ noflags))
((DstPt = 53) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((DstIpAddr = dns) ∧ (SrcPt = 67) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((SrcPt = 443) ⇒ (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 443))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 138))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 67) v (DstPt ≠ 80))
((SrcPt = 0) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((DstIpAddr = private_p2p) ∧ (DstPt = 137) ⇒ (Flags ≠ noflags))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 68))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 68))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((Proto = IGMP) ⇒ (Flags ≠ flags))
((Proto ≠ UDP) ⇒ (Flags = noflags) v (SrcPt ≠ 0))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt = 137) ⇒ (Bytes ≥ 64xPackets))
((SrcPt = 137) ∧ (DstIpAddr ≠ private_broadcast) ⇒ (Flags = flags))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ⇒ (DstIpAddr ≠ dns))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 53) v (SrcPt ≠ 67))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstPt = 53))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 22))
((Proto = UDP) ∧ (SrcPt = 0) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 80))
((SrcIpAddr ≠ dns) ⇒ (Flags ≠ flags) v (SrcPt ≠ 0))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 138))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstIpAddr = public_p2p))
((DstIpAddr = private_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 25))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 53))
((SrcPt ≠ 443) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (DstPt = 80))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 138))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 0) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((DstPt = 443) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 80))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((Proto ≠ UDP) ⇒ (Flags = flags) v (DstPt ≠ 80))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 67))
((Proto = IGMP) ∧ (SrcPt = 22) ⇒ (DstIpAddr = dns))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 80))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcIpAddr ≠ dns))
((SrcIpAddr = public_p2p) ⇒ (Bytes ≥ 42xPackets))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 443) v (Flags ≠ noflags))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((Proto = IGMP) ∧ (SrcPt = 53) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 68) ⇒ (Flags = noflags) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (Flags ≠ flags))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 53))
((Proto = TCP) ⇒ (DstPt ≠ 138))
((SrcPt = 68) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((Proto = ICMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 25))
((DstPt = 67) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 3))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 67))
((SrcPt = 22) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 3))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 138) ⇒ (DstIpAddr = public_p2p))
((SrcPt ≠ 138) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (SrcPt = 137))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 138))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 67))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 138))
((DstIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 25))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 138) v (Flags ≠ flags))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((DstPt = 53) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstIpAddr = private_broadcast))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ⇒ (SrcPt ≠ 138))
((Proto = ICMP) ⇒ (DstIpAddr = dns))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 22))
((Proto ≠ TCP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ dns))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 68))
((SrcPt = 0) ⇒ (DstIpAddr = dns) v (DstPt ≠ 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr = dns) v (DstPt ≠ 53))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 138))
((SrcPt = 80) ∧ (DstIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr = dns) ⇒ (DstPt = 3) v (DstPt = 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((SrcPt = 138) ∧ (Proto ≠ UDP) ⇒ (DstPt = 53))
((DstIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 22))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 138))
((DstPt = 80) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags ≠ flags))
((DstIpAddr = public_p2p) ∧ (Proto = ICMP) ⇒ (DstPt = 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (DstPt = 25))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 137))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (SrcPt = 53) v (SrcIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 443))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 0))
((DstIpAddr = private_broadcast) ∧ (Proto = IGMP) ⇒ (DstPt = 137))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 53))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstIpAddr = private_broadcast))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 0))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 25))
((DstIpAddr = public_p2p) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((SrcPt ≠ 80) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 53))
((Flags = noflags) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 137))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (DstPt ≠ 53))
((DstPt = 68) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((Flags ≠ flags) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 138) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ noflags))
((Proto ≠ ICMP) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 138) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((DstPt = 80) ∧ (SrcIpAddr = public_p2p) ⇒ (Flags = noflags))
((Proto = IGMP) ⇒ (DstPt ≠ 22))
((DstPt = 137) ∧ (SrcPt = 68) ⇒ (Flags = flags))
((DstIpAddr = public_p2p) ⇒ (65535xPackets ≥ Bytes))
((DstIpAddr = dns) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 137) v (SrcIpAddr ≠ dns))
((SrcIpAddr = dns) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 67))
((SrcIpAddr ≠ public_p2p) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 53))
((Proto = UDP) ⇒ (DstIpAddr = private_broadcast) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (DstPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (SrcIpAddr ≠ public_p2p))
((Proto ≠ UDP) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((SrcPt = 67) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (SrcPt ≠ 137))
((SrcPt = 80) ⇒ (DstPt ≠ 22))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 443))
((SrcIpAddr ≠ private_p2p) ⇒ (SrcPt ≠ 138))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 137))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 67))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((SrcIpAddr = public_p2p) ∧ (Proto ≠ TCP) ⇒ (SrcPt = 0))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 22) v (DstPt ≠ 138))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 0))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 22))
((SrcPt = 53) ⇒ (DstIpAddr ≠ dns))
((Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p) v (SrcIpAddr ≠ public_p2p))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (Flags = flags))
((DstPt = 25) ⇒ (Flags ≠ noflags))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 53))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (SrcPt ≠ 138))
((Proto ≠ TCP) ⇒ (DstPt ≠ 53) v (SrcPt ≠ 138))
((DstIpAddr = public_p2p) ∧ (SrcPt = 22) ⇒ (DstPt = 53))
((SrcPt ≠ 68) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 68))
((Proto ≠ TCP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 138))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ private_broadcast))
((DstIpAddr = private_p2p) ∧ (SrcPt = 137) ⇒ (Flags ≠ noflags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (Flags ≠ noflags))
((DstPt ≠ 0) ⇒ (Flags = noflags) v (Flags = flags))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 67))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ public_p2p))
((Proto = UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 138))
((DstPt = 53) ∧ (Proto = IGMP) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 443) v (SrcPt ≠ 443))
((DstIpAddr = private_broadcast) ∧ (Proto ≠ UDP) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 25))
((DstIpAddr = public_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 22))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 3))
((DstIpAddr = private_broadcast) ⇒ (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (Flags ≠ noflags) v (SrcPt ≠ 80))
((DstIpAddr = private_broadcast) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 138) ⇒ (Flags = noflags) v (DstPt ≠ 67))
((Proto = UDP) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 67))
((SrcIpAddr = public_p2p) ⇒ (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ dns) v (SrcIpAddr ≠ dns))
((SrcIpAddr = dns) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 25) v (DstPt ≠ 80))
((SrcIpAddr = dns) ⇒ (DstIpAddr ≠ public_p2p))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((DstIpAddr ≠ private_p2p) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((DstIpAddr = public_p2p) ∧ (SrcPt = 80) ⇒ (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 67) ⇒ (DstPt = 0))
((DstIpAddr = private_p2p) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 80))
((Proto ≠ IGMP) ⇒ (SrcIpAddr ≠ dns) v (SrcPt ≠ 22))
((SrcPt = 22) ⇒ (DstPt ≠ 137))
((Proto = UDP) ∧ (SrcPt = 80) ⇒ (DstIpAddr = dns))
((DstIpAddr = dns) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 67))
((Proto ≠ TCP) ⇒ (SrcIpAddr ≠ private_p2p) v (SrcIpAddr ≠ dns))
((SrcPt = 67) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt = 137) v (SrcPt ≠ 137))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 22))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 80))
((SrcPt ≠ 138) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Proto = UDP) ∧ (SrcPt = 22) ⇒ (DstPt = 80))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 67) v (SrcPt ≠ 138))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 0))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstIpAddr = dns))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 68) ⇒ (DstPt = 22))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 443))
((SrcPt ≠ 67) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ dns))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 22))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcPt = 53) ∧ (Proto ≠ UDP) ⇒ (DstPt = 138))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (DstPt = 53))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 25))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 67) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (Flags = flags))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 138))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (Flags ≠ noflags))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstPt = 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 53) v (DstPt ≠ 67))
((Proto = IGMP) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (SrcPt = 80))
((SrcPt = 67) ∧ (Proto ≠ UDP) ⇒ (DstIpAddr = private_broadcast))
((SrcPt ≠ 443) ⇒ (Flags = noflags) v (DstPt ≠ 0))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 22) v (DstPt ≠ 443))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 67))
((SrcIpAddr ≠ dns) ⇒ (SrcPt = 67) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 22))
((SrcPt ≠ 138) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((Proto ≠ IGMP) ⇒ (SrcPt ≠ 67) v (SrcPt ≠ 137))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 53) ⇒ (Flags ≠ noflags))
((SrcPt ≠ 443) ⇒ (DstPt ≠ 25) v (DstPt ≠ 443))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 68))
((SrcIpAddr = dns) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((Proto ≠ ICMP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 137))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstPt = 3))
((Proto = IGMP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 138))
((DstPt = 67) ∧ (SrcPt = 137) ⇒ (Flags = flags))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 80) v (Flags ≠ noflags))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 443) ⇒ (DstIpAddr = private_broadcast))
((Proto = UDP) ∧ (SrcIpAddr = public_p2p) ⇒ (DstIpAddr = private_broadcast))
((Proto = ICMP) ∧ (SrcPt = 80) ⇒ (DstPt = 25))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 25) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 53))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr = public_p2p) ⇒ (DstPt = 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ dns) v (SrcPt ≠ 137))
((SrcIpAddr = dns) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 25))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ public_p2p) v (DstPt ≠ 67))
((SrcIpAddr = dns) ⇒ (SrcPt ≠ 137))
((SrcPt = 68) ⇒ (DstIpAddr ≠ private_broadcast))
((Proto = IGMP) ∧ (SrcPt = 68) ⇒ (DstPt = 443))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstIpAddr ≠ private_p2p))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((Proto ≠ UDP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 443))
((SrcIpAddr = dns) ∧ (SrcPt = 0) ⇒ (DstPt = 53))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (DstPt = 67))
((DstIpAddr = private_broadcast) ∧ (Proto = TCP) ⇒ (DstPt = 25))
((SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 3))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 25) v (DstPt ≠ 67))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_broadcast) v (DstIpAddr ≠ public_p2p))
((SrcPt = 67) ⇒ (Flags = noflags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 68) v (Flags ≠ flags))
((Proto = TCP) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 80))
((SrcPt ≠ 137) ⇒ (DstPt ≠ 0) v (DstPt ≠ 3))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 0))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (Flags ≠ flags))
((SrcPt ≠ 80) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((DstIpAddr = private_broadcast) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((Proto = ICMP) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((DstIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((SrcIpAddr ≠ private_p2p) ⇒ (DstPt ≠ 53))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 25))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (SrcIpAddr ≠ public_p2p))
((DstIpAddr = public_p2p) ⇒ (DstPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt = 0) v (DstPt ≠ 3))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (DstPt ≠ 443))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 138))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 443))
((DstPt = 443) ⇒ (Flags = flags))
((Proto = UDP) ⇒ (DstPt = 67) v (SrcPt ≠ 68))
((Proto ≠ UDP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((DstPt = 0) ∧ (Proto = UDP) ⇒ (Flags ≠ noflags))
((Proto ≠ UDP) ⇒ (SrcIpAddr ≠ public_p2p) v (SrcPt ≠ 0))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 443))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 22) v (Flags ≠ noflags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((Proto = IGMP) ∧ (DstIpAddr ≠ public_p2p) ⇒ (DstPt = 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 25) v (SrcPt ≠ 0))
((Proto = IGMP) ⇒ (Bytes ≥ 42xPackets))
((Proto ≠ TCP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 137))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 443))
((DstIpAddr ≠ dns) ⇒ (Flags = noflags) v (Flags = flags))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 0) ⇒ (DstPt = 53))
((SrcPt = 22) ∧ (Proto ≠ TCP) ⇒ (DstPt = 67))
((SrcPt ≠ 0) ⇒ (Flags = noflags) v (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 68))
((Proto ≠ TCP) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 67))
((Proto ≠ ICMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 68) v (DstPt ≠ 443))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags ≠ flags))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (SrcPt ≠ 0))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 80) v (DstPt ≠ 138))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((Proto = ICMP) ∧ (SrcPt = 53) ⇒ (DstPt = 68))
((SrcPt ≠ 67) ⇒ (Flags = flags) v (DstPt ≠ 443))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_broadcast) v (SrcPt ≠ 137))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 137))
((Proto = TCP) ∧ (SrcPt = 67) ⇒ (DstIpAddr = dns))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 3) v (SrcPt ≠ 53))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 80) ⇒ (DstPt = 137))
((DstIpAddr ≠ dns) ⇒ (65535xPackets ≥ Bytes))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstPt = 22))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 137))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 67) v (DstPt ≠ 443))
((Proto = UDP) ⇒ (DstPt ≠ 137) v (SrcPt ≠ 138))
((SrcPt = 53) ∧ (SrcIpAddr ≠ dns) ⇒ (DstIpAddr ≠ private_p2p))
((SrcPt ≠ 68) ⇒ (DstPt ≠ 3) v (DstPt ≠ 80))
((Proto ≠ ICMP) ⇒ (SrcPt ≠ 0) v (SrcPt ≠ 67))
((Proto ≠ TCP) ⇒ (SrcIpAddr = private_p2p) v (DstIpAddr ≠ public_p2p))
((SrcPt = 443) ∧ (SrcIpAddr ≠ public_p2p) ⇒ (DstPt = 137))
((SrcPt ≠ 137) ⇒ (65535xPackets ≥ Bytes))
((SrcPt ≠ 22) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 25))
((DstPt ≠ 137) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcIpAddr = private_p2p) ⇒ (Flags ≠ flags) v (SrcPt ≠ 138))
((SrcPt = 22) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 137))
((SrcPt = 67) ⇒ (DstPt ≠ 22))
((DstIpAddr = private_broadcast) ∧ (SrcPt = 443) ⇒ (DstPt = 67))
((DstIpAddr ≠ private_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((Proto ≠ UDP) ⇒ (DstPt ≠ 0) v (Flags ≠ flags))
((Proto = IGMP) ∧ (SrcPt = 67) ⇒ (Flags = flags))
((DstIpAddr ≠ dns) ⇒ (DstPt ≠ 137) v (DstPt ≠ 443))
((Proto = ICMP) ∧ (SrcPt = 443) ⇒ (DstIpAddr = private_broadcast))
((SrcPt = 53) ⇒ (DstPt ≠ 67))
((Proto = IGMP) ⇒ (DstPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 0))
((SrcPt = 138) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 3))
((SrcIpAddr ≠ public_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 3))
((SrcIpAddr ≠ dns) ⇒ (SrcPt ≠ 80) v (SrcPt ≠ 137))
((SrcPt ≠ 80) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 25))
((Proto ≠ IGMP) ⇒ (DstPt = 3) v (SrcPt ≠ 0))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 138) v (DstPt ≠ 443))
((SrcPt ≠ 53) ⇒ (DstIpAddr ≠ dns) v (DstPt ≠ 80))
((Proto ≠ UDP) ⇒ (DstPt ≠ 3) v (DstPt ≠ 22))
((Proto ≠ ICMP) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 68))
((SrcIpAddr = dns) ⇒ (DstPt ≠ 68))
((SrcIpAddr = public_p2p) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 68))
((SrcPt ≠ 443) ⇒ (DstIpAddr ≠ private_p2p) v (DstPt ≠ 443))
((Proto ≠ IGMP) ⇒ (SrcPt = 53) v (SrcIpAddr ≠ dns))
((SrcPt ≠ 67) ⇒ (DstPt ≠ 3) v (DstPt ≠ 443))
((DstIpAddr = private_p2p) ∧ (SrcPt = 138) ⇒ (DstPt = 80))
((Proto = IGMP) ⇒ (SrcIpAddr ≠ dns))
((SrcIpAddr = public_p2p) ∧ (SrcPt = 0) ⇒ (DstPt = 67))
((SrcPt ≠ 22) ⇒ (DstPt ≠ 22) v (DstPt ≠ 68))
((DstIpAddr = dns) ∧ (SrcPt = 138) ⇒ (DstPt = 137))
((DstIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 137))
((DstPt ≠ 68) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((Proto = ICMP) ∧ (SrcIpAddr = dns) ⇒ (Flags ≠ noflags))
((Proto = TCP) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ dns))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_broadcast) v (SrcPt ≠ 22))
((Proto ≠ ICMP) ⇒ (Flags = noflags) v (DstIpAddr ≠ private_broadcast))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr = public_p2p) v (DstPt ≠ 443))
((DstIpAddr = public_p2p) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (Flags = flags))
((Proto = UDP) ⇒ (DstPt ≠ 138) v (SrcPt ≠ 137))
((Proto = IGMP) ∧ (SrcPt ≠ 0) ⇒ (DstPt = 3))
((DstIpAddr = dns) ∧ (SrcPt = 137) ⇒ (DstPt = 3))
((SrcPt ≠ 0) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ private_broadcast))
((Proto ≠ TCP) ⇒ (SrcPt ≠ 22))
((SrcIpAddr ≠ public_p2p) ⇒ (DstPt ≠ 68) v (DstPt ≠ 80))
((Proto ≠ IGMP) ⇒ (SrcIpAddr = private_p2p) v (DstPt ≠ 53))
((SrcPt ≠ 137) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 0))
((Proto ≠ ICMP) ⇒ (DstIpAddr ≠ public_p2p) v (SrcPt ≠ 22))
((SrcPt = 137) ∧ (SrcIpAddr ≠ private_p2p) ⇒ (DstPt = 53))
((SrcPt ≠ 0) ⇒ (DstPt ≠ 53) v (DstPt ≠ 137))
((SrcIpAddr = private_p2p) ∧ (SrcPt = 53) ⇒ (DstPt = 25))
((Proto ≠ IGMP) ⇒ (DstIpAddr = private_p2p) v (DstPt ≠ 68))
((DstIpAddr = public_p2p) ∧ (DstPt = 68) ⇒ (Flags = flags))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 0) v (DstPt ≠ 68))
((SrcIpAddr ≠ dns) ⇒ (DstPt ≠ 68) v (SrcPt ≠ 138))
((SrcIpAddr ≠ public_p2p) ⇒ (SrcPt ≠ 22) v (SrcPt ≠ 53))
((DstIpAddr = dns) ∧ (DstPt = 80) ⇒ (Flags = noflags))
((Proto = UDP) ⇒ (DstPt ≠ 3))
((SrcIpAddr = dns) ∧ (SrcPt = 443) ⇒ (DstPt = 80))
((SrcPt = 68) ⇒ (DstPt ≠ 138))
((DstIpAddr = private_broadcast) ∧ (Proto = ICMP) ⇒ (Flags = flags))
((SrcPt = 68) ∧ (DstIpAddr ≠ private_p2p) ⇒ (DstPt = 22))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 67) v (DstPt ≠ 137))
((DstIpAddr = public_p2p) ∧ (SrcPt = 443) ⇒ (Flags = noflags))
((SrcPt ≠ 53) ⇒ (DstPt ≠ 0) v (DstPt ≠ 67))
((SrcIpAddr = private_p2p) ⇒ (DstIpAddr ≠ private_p2p) v (DstIpAddr ≠ public_p2p))
((Proto ≠ ICMP) ⇒ (DstIpAddr = private_p2p) v (SrcPt ≠ 67))
((Proto ≠ IGMP) ⇒ (DstIpAddr ≠ private_broadcast) v (DstPt ≠ 443))
((DstPt ≠ 67) ⇒ (Flags ≠ noflags) v (Flags ≠ flags))
((DstIpAddr ≠ private_broadcast) ⇒ (DstPt ≠ 3) v (Flags ≠ flags))
((DstIpAddr = dns) ⇒ (DstPt ≠ 3) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (Flags = flags) v (DstPt ≠ 53))
((Proto ≠ UDP) ⇒ (DstPt ≠ 22) v (SrcPt ≠ 80))
((SrcIpAddr = private_p2p) ⇒ (DstPt ≠ 22) v (DstPt ≠ 67))
